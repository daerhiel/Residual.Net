namespace ResidualNet.Containers
{
    /// <summary>
    /// Represents the enumeration that define pussible insertion order states.
    /// </summary>
    public enum InsertPosition
    {
        /// <summary>
        /// The node should be inserted as a preceding to the origin node.
        /// </summary>
        Preceding,

        /// <summary>
        /// The node should de inserted as a following to the origin node.
        /// </summary>
        Following
    }
}